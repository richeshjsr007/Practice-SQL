-- Adding new records to table first 

SELECT DATABASE();
describe books;
INSERT INTO books
    (title, author_fname, author_lname, released_year, stock_quantity, pages)
    VALUES ('10% Happier', 'Dan', 'Harris', 2014, 29, 256), 
           ('fake_book', 'Freida', 'Harris', 2001, 287, 428),
           ('Lincoln In The Bardo', 'George', 'Saunders', 2017, 1000, 367);

-- Topic 1. DISTINCT ( Display UNIQUE data from table)

-- Example :-  'Display unique last names for Authors'

SELECT DISTINCT
    (author_lname)
FROM
    BOOKS;

-- Example :- 'Display unique full names for Authors'

SELECT DISTINCT
    author_fname, author_lname
FROM
    BOOKS;

OR

SELECT DISTINCT
    CONCAT(author_fname, ' ', author_lname) as 'Full name'
FROM
    BOOKS;

-- Topic 2. ORDER BY ( Sorting O/P data from table)

-- Display all records from the books table, sorted by title.

SELECT 
    *
FROM
    books
ORDER BY TITLE;  

-- Another way - Display all records from the books table, ordered by title from Z to A.

SELECT 
    *
FROM
    books
ORDER BY title DESC;

-- Another way - Display all records from the books table, ordered by title. 

SELECT 
    *
FROM
    books
ORDER BY 2;

-- Explaination -  ORDER BY 2 - means order by '2nd Column from the SELECT query'

OR 

SELECT 
    author_lname, stock_quantity, pages, title
FROM
    books
ORDER BY 4;

--  3. ORDER BY using MULTIPLE Columns 

-- Display all records from the books table, ordered by author_fname and released year.

SELECT 
    *
FROM
    books
ORDER BY author_fname, released_year;

OR

-- Display title, author first name & released year from the books table, ordered by author first name and newest released year.

SELECT 
    title, author_fname, released_year
FROM
    books
ORDER BY author_fname , released_year DESC;

OR 

-- 4. ORDER BY using Aliases
-- Display Authors full name and sort by fullname .

SELECT 
    CONCAT(author_fname, ' ', author_lname) AS Authors
FROM
    books
ORDER BY Authors;

-- 4. Order by using LIMIT (It controls the no. of rows in the result set)
-- Example :-  Display the first 8 records from the books table, sorted by their ID.

SELECT 
    *
FROM
    BOOKS
ORDER BY BOOK_ID
LIMIT 8;

Example:-  Display the 10 earliest released books from the books table.

SELECT 
    *
FROM
    BOOKS
ORDER BY released_year
LIMIT 10
;

OR

SELECT 
    *
FROM
    BOOKS
ORDER BY book_id
LIMIT 3,5
;
-- Explanation -  In above query ORDER BY book_id LIMIT 3,5 -  means skip the first 3 rows of the sorted results and display the next 5 records. 
-- (This allows us to SLECT SUBSET in between grouping)

-- 5. Order by using LIMIT and WHERE Clause (WHERE is used as filter)
-- Display the first two records from the books table where the author's first name is 'Neil', sorted by title.

SELECT * 
FROM books 
WHERE author_fname = 'Neil' 
ORDER BY title ASC 
LIMIT 2;

-- 6. LIKE Operator (For better searching)
-- USING - LIKE '%D%' -  means search for records where the column contains the letter 'D' anywhere inside the text (at the beginning, middle, or end).
   The % symbol represents any number of characters surrounding it. ' % ' is used as WildCard.

-- Example :- Display title, author's first name and release year from books where title contain ':'.

SELECT 
    title, author_fname, released_year
FROM
    books
WHERE
    title LIKE '%:%'
;

OR
-- Display the book title, author's first name, and release year from the books table for all books released in the 1900s.

SELECT 
    title, author_fname, released_year
FROM
    books
WHERE
    released_year LIKE '19%'
;

-- ** We can also use '_' as wildcard to give exact number for characters we are looking in the Result set.

-- Example :-  Display title, author_fname, released_year from books table where author's first name is having 5 characters.

SELECT 
    title, author_fname, released_year
FROM
    books
WHERE
    author_fname LIKE '_____';

OR

-- Display title, author_fname, released_year from books table where author's first name is having 5 characters and ending with 'id'.

SELECT 
    title, author_fname, released_year
FROM
    books
WHERE
    author_fname LIKE '___id';

-- ** We can also use '\' along with % sign if data contain any special characters.
-- Example - Display title, author_fname, released_year from books table where title containing '%'.

SELECT 
    title, author_fname, released_year
FROM
    books
WHERE
    title LIKE '%\%%';

-- Practice Task .
-- 1. Display a collection of books containing 'stories' in their titles.

SELECT 
    title
FROM
    books
WHERE
    title LIKE '%stories%';

-- 2. Find the longest book from the collection and display the title and no. of pages

 SELECT 
    title, pages
FROM
    books
ORDER BY pages DESC
LIMIT 1;

-- 3. Display a summary contaning the title and year, for 3 most recent books

SELECT 
    CONCAT(title, '-', released_year) AS Summary
FROM
    books
ORDER BY released_year DESC
LIMIT 3;

-- 4. Display all the books with authors lastname that contains a space.

SELECT 
    author_lname
FROM
    books
WHERE
    author_lname LIKE '% %';

-- 5. Display 3 books with lowest stocks with their title, released year and stock count.

SELECT 
    title, released_year, stock_quantity
FROM
    books
ORDER BY stock_quantity
LIMIT 3;  

--6. Display title and authors last name from books table and sort by authors last and then by title.

SELECT 
    title, author_lname
FROM
    books
ORDER BY author_lname , title;
